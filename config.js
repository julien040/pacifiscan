import { Heading } from "native-base";

const appName = "Pacifiscan"

const Logo =  () => (
    <Heading fontSize={22} color="brand.logo" letterSpacing={-0.5}>
            Pacifiscan
    </Heading>
)

export {
    appName,
    Logo
}